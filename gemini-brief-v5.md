# 龙卷课堂PPT · Gemini 补充项目书 v5

## 任务G：地图背景大陆轮廓 + 经纬网

### 需求
为所有涉及地图的页面提供隐约可见的地理背景元素：
- S10 全球分布：简化世界各大洲轮廓 + 经纬网线
- S11 中国龙卷：完整的中国海岸线 + 30°N/40°N/110°E/120°E 经纬参考线
- 章节过渡页（形成机理/结构解剖）：淡化的北半球简化轮廓

### 技术要求
- 各大陆用极简路径（`<path>`），不要精确国界，能辨认大致形状即可
- 经纬网线：`<line>` 元素，`stroke="rgba(93,184,232,0.06)" stroke-width="0.5"`
- 大陆填充：`fill="rgba(59,140,192,0.04)"`，描边：`stroke="rgba(93,184,232,0.12)"`
- 每个地图独立 SVG，viewBox 自适应
- 输出完整的 `<svg>` 标签

### 交付格式
三个独立 SVG 代码块，标注用途（S10/S11/章节页）。

---

## 任务H：Part 过渡页毛玻璃云纹增强

### 需求
增强 `.slide.chapter::before` 和 `::after` 的毛玻璃质感，参考"低空气象眼"项目的美术风格——多层半透明流体叠加 + 磨砂噪点 + 玻璃边缘反光。

### 技术要求
- `::before`：云团光斑从4个增加到6个，动画幅度加大（opacity 0.2↔0.95），周期错开
- `::after`：增加 SVG feTurbulence 噪点纹理叠加（或 CSS `repeating-conic-gradient` 极细条纹模拟磨砂表面）
- 新增玻璃边缘反光：`inset box-shadow` 或渐变边框光晕
- 保持 `backdrop-filter: blur(16px)`
- 所有颜色使用现有气象蓝系统：锚点 #4DA8DA、亮青 #5DB8E8、冰蓝 #B0DCF6

### 交付格式
纯 CSS 代码块（替换 `.slide.chapter::before` 和 `::after` 规则 + 对应的 `@keyframes`）。

---

## 任务I：S10 强化世界地图SVG

### 需求
在任务G的简化大陆轮廓基础上，做一个完整可用的S10世界地图SVG，包含：
- 简化大陆轮廓（任务G提供）
- 六大龙卷高发区呼吸光斑（保留现有SMIL动画）
- Tornado Alley 最大金色光斑 + 标注
- 经纬参考线（30°N/30°S/赤道/本初子午线，虚线）
- viewBox="0 0 800 400"

### 交付格式
完整 `<svg>` 标签，可直接嵌入S10替换现有地图。
